name: Build Pdf

on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo apt install -y wkhtmltopdf
      - run: wkhtmltopdf -v
